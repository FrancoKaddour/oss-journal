# 2026-07-20

## Merged — second supabase-js fix ✅

[#2487](https://github.com/supabase/supabase-js/pull/2487) (clean up cross-signal abort listener on `invoke()` return, in functions-js) was approved and merged by the maintainer the same day the review came in. Second merged fix in supabase-js — the first ([#2490](https://github.com/supabase/supabase-js/pull/2490)) already shipped in v2.110.6.

## TanStack Start — minimal repro for #7837 (prerender crawler corrupts query strings)

The maintainer had asked for a minimal reproduction and the issue sat in "information needed" for three days. Built one: [tanstack-start-prerender-7837-repro](https://github.com/FrancoKaddour/tanstack-start-prerender-7837-repro).

Findings beyond the original report:

- **Corruption confirmed end-to-end.** The published `@tanstack/start-plugin-core` calls `withTrailingSlash` without `respectQueryAndFragment`, so the crawler requests `/?currency=USD/`; serving that request renders corrupted state (`Currency: USD/`) and emits the next-generation corrupted link (`href="/?currency=USD%2F"`).
- **Why the infinite loop is hard to reproduce minimally (new finding):** instrumenting SSR showed only the *first* crawl reaches the server — the crawler's subsequent requests (including corrupted ones) get served the just-written static `index.html` (queries ignored), so a small app converges instead of hanging. The loop needs corrupted requests to keep reaching SSR before static files shadow them.
- **Related output artifact:** all query-variant pages strip the query when computing the output path and overwrite the same `index.html`, while being reported as separately prerendered.

Posted the repro + analysis: [TanStack/router#7837](https://github.com/TanStack/router/issues/7837#issuecomment-5024448817). Offered a PR with regression test.

## Storybook

[#35528](https://github.com/storybookjs/storybook/pull/35528) stays in draft for now: the repo limits open (non-draft) PRs per external author, and [#35527](https://github.com/storybookjs/storybook/pull/35527) is already in review. It flips to ready as soon as the first one resolves.

Also connected the dots on [#35502](https://github.com/storybookjs/storybook/issues/35502) (Zag focus-visible breaking under the 10.5 instrumentation): same root cause as #35503, fixed by #35528. Posted the analysis with one honest caveat — the fix makes prototype reads safe but the property remains an accessor, so an "is a native data property" assertion still fails. [Comment](https://github.com/storybookjs/storybook/issues/35502#issuecomment-5024628775).

## Vite — first contribution: triage of #22968 (`buildStart` runs twice in bundled dev)

Reproduced the bug (reported from TanStack Start) on Windows and sharpened the characterization: it's not an overlap race — the duplicate `buildStart` fires on the *first module-graph access whenever it happens*, because the regular client plugin container never learns the Rolldown lifecycle already ran. Traced the exact path: `initServer` skips the eager container `buildStart` under `bundledDev`, but `EnvironmentPluginContainer.resolveId`'s lazy start reintroduces it. Suggested a direction using the container's existing `autoStart` mechanism, deferring to the team. [Comment](https://github.com/vitejs/vite/issues/22968#issuecomment-5024732864).
