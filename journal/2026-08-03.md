# 2026-08-03 — Five PRs to Ready + Floating Panel stacking fix (zag#3246)

Monday kickoff: the whole weekend batch went from draft to ready-for-review — [storybook#35706](https://github.com/storybookjs/storybook/pull/35706), [router#7937](https://github.com/TanStack/router/pull/7937), [router#7938](https://github.com/TanStack/router/pull/7938), [form#2260](https://github.com/TanStack/form/pull/2260) and [form#2261](https://github.com/TanStack/form/pull/2261).

CodeRabbit reviewed the form PRs within minutes; both findings answered with code inside the hour. The one on #2261 was a real catch: `evaluate()` delegated to a custom `equals()` checking only one operand, so `evaluate({ equals: () => true }, {})` — two plain object literals sharing the `Object` constructor — would report structurally different values as equal. Now both operands must expose a callable `equals`, with a one-sided regression test.

## [zag#3246](https://github.com/chakra-ui/zag/pull/3246) — Floating Panel stacking (closes #3242 + #3243)

One PR, two bugs, one component:

- **Stale stack on close (#3243):** panels joined the shared `panelStack` on open but only left it on machine *destroy*. Closing the topmost panel left it marked `data-topmost` while the remaining panel stayed `data-behind`. Fix: the `closed` state now removes the panel from the stack on entry; reopening re-adds it through the existing open-entry action.
- **Stack order never reached the positioner (#3242):** the stack index (`--z-index`) was written only to the content element, but each positioner is a sibling stacking context — a content z-index can never reorder panels relative to each other. Fix: apply the index to the positioner and consume it via `zIndex: "var(--z-index)"` in `getPositionerProps`.

Shipped with a new two-panel `stacking` example page and two e2e regression tests asserting `data-topmost` and computed positioner z-index. Red-green verified locally: both tests fail on `main`, pass with the fix (package rebuilt in each direction).

## Also today

- Wide scout across 13 repos: 13 of ~18 fresh candidates already had competing PRs — vite and base-ui burn in under 48h. The honest shortlist (vitest#10860, vite#23143) is parked for next sessions.
- Watching vite#23007: the reporter pushed back on the "expected behavior" close; if the maintainer concedes the docs note I proposed, that docs PR is the natural next step.
