# 2026-08-04 — Zag #3246 MERGED by Segun + two new orgs entered in one day

## Merge of the week

**[zag#3246](https://github.com/chakra-ui/zag/pull/3246) merged by Segun Adebayo** — one PR closing two Floating Panel stacking issues (#3242 + #3243), with Segun refining directly on my branch before merging (bindable `stackIndex`, open-state `exit`). Second merge in zag.

Also this morning: applied the midudev team's review on [spiderman#62](https://github.com/midudev/spiderman-brand-new-day/pull/62) within the hour (X moved to the screen corner via `position: fixed`, which escapes the dialog's overflow clip since `.trailer-modal` carries no transform/filter) — merge promised on re-review.

## Two new orgs in one day

**[vitest-dev/vitest#10873](https://github.com/vitest-dev/vitest/pull/10873)** — jsdom/happy-dom lost `localStorage`/`sessionStorage` on Node 25+ (the new native lazy accessors collide with the window-key allowlist). The fix already existed on `main` for v5; the contribution is the disciplined part: a faithful backport of the environment portion of #10293 to the stable `v4` line per the repo's release-branch policy, with a test that simulates Node 25's lazy accessors so it runs red/green on any Node version. 6/6 green.

**[adobe/react-spectrum#10424](https://github.com/adobe/react-spectrum/pull/10424)** — Gregorian → Ethiopic/Coptic conversion reported New Year's Day as the previous year's last day, 3 of every 4 years. Proved by algebra that the year estimate `floor(4(jd−epoch)/1461)` equals `Y + floor((4k − r)/1461)` — exactly one year low when `k = 0, r ≠ 0`, never otherwise — so the reporter's single-correction fix is provably sufficient and touches only the broken days. Credited the fix to the reporter; added new-year boundary tests (ethiopic/ethioaa/coptic, leap boundary, AM 1-1-1 era boundary) and 8-year round-trips. 83/83 green, red-green: exactly the 5 new tests fail on main.

Killed honestly along the way: vitest#10860 (another contributor claimed it with a plan hours earlier) and vite#23143 (competing PR appeared within 24h — vite burns fast, again).

## Evening update — spiderman#62 MERGED

[spiderman-brand-new-day#62](https://github.com/midudev/spiderman-brand-new-day/pull/62) merged by marp0604 less than 24 hours after opening — the midudev front's first merge, live on the official Spider-Man: Brand New Day site. Full cycle: bug measured on production → fix → team review applied within the hour → merged by a second team member. Two merges today (zag#3246 by Segun Adebayo, spiderman#62), two new orgs entered (vitest, adobe).
