# 2026-07-22

## Environment day — WSL2 up, caveat removed from #35544

Set up WSL2 (Ubuntu 26.04, node 22, corepack) to fix the recurring Windows problem: test suites that hang locally forced "couldn't verify locally" caveats into two PRs. The payoff was immediate:

- storybook `yarn install`: **3m34s** on WSL vs 51min on Windows
- `test-manager.test.ts`: **18/18 in 6.6s** on WSL vs infinite hang on Windows
- Red-green verified for [#35544](https://github.com/storybookjs/storybook/pull/35544): reverting only the fix makes exactly the two new coverage assertions fail

Updated the PR body replacing the transparency caveat with the verified results. Also added a `C:\gh` junction to dodge the space-in-path bug that broke local linting.
